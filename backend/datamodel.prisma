type User {
	id: ID! @unique
	name: String!
	email: String!
	# keyboards: [Keyboard]
}

type Keyboard {
	id: ID! @unique
	name: String!
	switches: String!
	size: String!
	image: String
	layout: String
	price: Int!
	description: String
	keycaps: String
	# createAt: DateTime!
	# updatedAt: DateTime!
	# user: User!
}
