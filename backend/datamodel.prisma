type User {
	id: ID! @unique
	name: String!
	email: String!
	# keyboards: [Keyboard]
}

enum Condition {
	USED
	NEW
}

type Keyboard {
	id: ID! @unique
	name: String!
	switches: String!
	size: String!
	images: [String!]!
	layout: String
	price: Int!
	description: String
	keycaps: String
	condition: Condition!
	# createAt: DateTime!
	# updatedAt: DateTime!
	# user: User!
}
