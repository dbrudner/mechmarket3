# import * from './generated/prisma.graphql'

type Mutation {
	createKeyboard(
		name: String!
		switches: String!
		size: String!
		image: [String]
		layout: String
		price: Int!
		description: String!
		keycaps: String
	): Keyboard!
}

type Query {
	keyboards: [Keyboard]!
}
